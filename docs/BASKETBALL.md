# Basketball research desk

Native Next.js pages now live at `/basketball/`, `/basketball/learn/`, `/basketball/matchups/`, `/basketball/gameplan/`, `/basketball/ratings/`, `/basketball/conferences/`, `/basketball/players/`, `/basketball/player-profiles/`, `/basketball/ncaa-player-box/`, `/basketball/source-stats/`, `/basketball/impact/`, `/basketball/recruiting/`, `/basketball/roster-lab/`, `/basketball/player/`, `/basketball/model/`, `/basketball/pressroom/`, `/basketball/film/` and `/basketball/season/`. Per-game basketball briefs appear at `/basketball/briefs/:id/` and in the shared journal. The legacy `/film/`, `/conferences/`, `/scout/`, `/rankings/`, `/teams/`, `/games/` and `/pressroom/` links now land on native pages; unknown archive deep links continue through the preserved TanStack shell for compatibility.

The native `/basketball/ncaa/` page adds a separate NCAA identity namespace for final national individual-stat snapshots across Divisions I, II and III. The coach-facing `/basketball/gameplan/` route now runs on the native matchup workbench; other preserved archive routes remain available through the legacy shell until their native replacements are published. See [`NCAA_INDIVIDUAL.md`](NCAA_INDIVIDUAL.md) for source, cache and D1 publication details.

The [reading room](/basketball/learn/) includes browser-only lessons for Four Factors, market probability and recruiting workload. Its searchable metric explorer groups the definitions by team, player, impact, recruiting and market topics and links each definition to the evidence desk where it can be inspected. The calculators apply disclosed denominators, convert American prices into raw and no-vig probabilities, and preserve unavailable results when an input is missing; calculator values never enter the model, market archive or storage.

## Current verified import

- 26,756 schedule records across season-ending years 2023–2027.
- 25,041 completed games with usable paired team box scores; 22 other completed games excluded from efficiency calculations.
- 799,620 identified 2023–26 player box-score rows, with 444,505 source rows preserved separately when required IDs are missing. The coverage desk breaks that queue down by dataset and shows that 444,461 of those rows still carry source observations, while none is attributed to a guessed player or team.
- The [identity review queue](/basketball/identity-review/) exposes bounded, searchable pages of those retained source observations through a read-only D1 endpoint. It shows the original source fields, dataset, season, row index and withholding reason, and offers a page-level CSV download; it never turns a name-only match into an identity.
- 9,990 player/team entries with recorded minutes in the player index; incomplete box-score aggregates are not presented as complete rates.
- 2025 and 2026 publisher player-season releases (416,788 and 426,040 source rows) are grouped into 19,822 player/team records, retaining labels, values, displays and descriptions.
- 2024, 2025 and 2026 publisher team-season releases (32,265, 31,500 and 32,715 source rows) are grouped into 717, 700 and 727 team records, retaining 45 labeled aggregate fields for the team-stat browser.
- The attributed `mbb_ratings` release adds 13,069 team-rating rows with adjusted offense, defense, net efficiency, tempo and publisher ranks across 2006–26. The companion `mbb_player_value` release adds 160,031 player/team rows with Box Plus/Minus, offensive BPM, defensive BPM and recorded minutes across the same seasons.
- The NCAA-derived lineup releases add 2,075,129 source stints across season-ending years 2019–26, aggregated into 599,160 lineup-key records with player names, possessions, scoring, opponent scoring and shot profile. Names stay in the NCAA lineup namespace and are not joined to ESPN IDs.
- The ESPN-derived `player_core` release adds 202,479 source identity profiles across 24 seasons (2002–03 through 2025–26), including names, positions, listed size, jersey, experience and status fields. Personal birth-date, age and birth-location fields are omitted from public profiles.
- 4,974 NCAA league-wide RAPM records, kept in their original NCAA identity namespace.
- The NCAA-derived warehouse retains 1,771,275 player-game rows for every completed season from 2010–11 through 2025–26. The published [NCAA player box archive](/basketball/ncaa-player-box/) serves the current 2025–26 game rows plus historical player-season summaries, retaining counting stats and shooting, transition and playmaking splits. It exports both the visible page and every matching filtered row with raw source JSON and canonical NCAA player/contest URLs; a bounded browser window protects the client, while the exact season Parquet release remains available. The individual player card carries the same links into its complete game-log CSV. Derived TS%, shooting rates and composite totals such as rebounds require every source component or denominator and preserve recorded zeroes, so an incomplete source row remains unavailable rather than becoming a fabricated value. The release is kept in its separate NCAA identity namespace; no name-only join to ESPN records is used.
- The NCAA player box page also exposes a source-parquet download for each 2010–11 through 2025–26 edition. Downloads are served from the private R2 research archive using the recorded release hash and remain attributed to SportsDataverse; the interactive page stays paginated and D1-backed.
- The NCAA player box archive now publishes a field-level audit beside the browser. For each season it counts every retained source key and its observed share, including zeroes as observed values; null and blank fields remain unavailable. This makes sparse advanced measures visible before they are used in a ranking or comparison.
- The [NCAA player rankings](/basketball/ncaa-rankings/) aggregates those rows by player/team and supports points, rebounds, assists, steals, blocks, true shooting, effective field goal percentage and points per 40 minutes, with explicit minimum-game, minimum-minute, source-position and source-class filters to prevent sparse rows from leading efficiency boards. Its page and complete CSV exports retain canonical NCAA player and team source URLs beside the stable IDs, so an offline shortlist keeps its audit path. Coach quick lenses open the all-around, verified-impact, creator, shot-efficiency or defensive-event view with documented thresholds; the balanced and impact indices retain component/possession audits. Position and class filters use exact NCAA player/team/season roster IDs; missing roster metadata is not imputed.
- The attributed publisher stat browser keeps its season, source field, direction, search and page controls in the URL and offers a copy-link action, so a reader can hand off an exact source-field slice for audit. Each selected edition also shows its source release URL, retrieval time and SHA-256 receipt beside the rows.
- The NCAA player rankings board also offers a descriptive balanced production index. It standardizes the available points, rebounds, assists, steals, blocks, true shooting, effective field goal and points-per-40 components within the filtered cohort, requires at least four components, and averages only observed components. Each row exposes the full-cohort percentile, available-component count and raw component audit (games for counting rates, FGA + 0.475 × FTA for TS%, FGA for eFG% and minutes for P40); the page CSV carries those values and denominators. The impact + production index combines standardized exact-ID net RAPM with points per 40 when both sources are available, and exposes the underlying Net RAPM, P40 and possession qualification. Both are shortlist aids, not eligibility grades, identity crosswalks or forecast inputs.
- The NCAA player rankings board also supports exact-ID net RAPM ordering. It joins the NCAA lineup-impact release to NCAA box rows only on season/player ID, and exposes ORAPM, DRAPM and offensive/defensive possession samples beside the rank. Players without an impact record remain unavailable for that metric rather than receiving a zero or a name-based match. The season source panel includes each retained release URL, retrieval clock and hash for direct provenance checks, and the bounded full-export action downloads every matching qualified row with the active filters. Turnover-rate boards rank the lowest recorded rates first because lower is the favorable coaching direction; the API returns the active direction beside the metric.
- The NCAA player rankings board also supports exact-ID ORAPM and DRAPM ordering. These offense-only and defense-only boards require at least 500 offensive and 500 defensive possessions, alongside the selected box-score games/minutes thresholds, so sparse lineup estimates do not lead the list.
- The NCAA player rankings board also exposes assist-to-turnover ratio and steals-plus-blocks per 40 minutes from the recorded box fields. Zero-turnover and missing-minute denominators remain unavailable rather than being treated as zero.
- The NCAA player rankings board also exposes turnover rate (recorded turnovers divided by recorded offensive possessions) and three-point attempt rate (3PA divided by FGA). Missing source denominators remain unavailable; these are descriptive source rates, not forecast inputs.
- The NCAA player rankings board also exposes three-point accuracy (3PM/3PA), free-throw attempt rate (FTA/FGA), assists per recorded possession, points per recorded possession and offensive, defensive and total rebounds per 40 minutes. These are source-derived descriptive rates with explicit denominators; they are not forecast inputs.
- The NCAA player rankings board also exposes team possession share: recorded player offensive possessions divided by the sum of recorded player possessions for that NCAA team-season. It is a workload context measure, not a usage projection or forecast feature. Rate boards support a matching minimum-denominator filter (FGA, turnovers or possessions) so small samples can be excluded explicitly.
- The NCAA player rankings board also exposes rim attempt rate (rim attempts/FGA), transition scoring share (transition points/points) and unassisted scoring share (unassisted points/points). These source-native shot-profile and creation descriptors use matching FGA or point denominators and remain unavailable when those denominators are absent; they are not role projections or forecast inputs.
- NCAA ranking and game rows include a direct link to the corresponding publisher player page, alongside the internal archive link, so a displayed source record can be checked against the NCAA page without treating the handoff as a verified identity join.
- The [NCAA historical leaderboard](/basketball/ncaa-careers/) compares player-season rows across a selectable 2010–11 through 2025–26 window. It ranks total and per-game production with minimum-game and minimum-minute filters, and can export every matching row in the bounded filtered cohort with canonical NCAA player/team source URLs. The release has no stable cross-season player crosswalk, so rows remain separate by season and NCAA ID rather than using an unverified name join.
- The [NCAA roster intel](/basketball/ncaa-rosters/) archive adds 92,220 attributed team-roster rows from 2010–11 through 2025–26 with NCAA player IDs, class, position, height, hometown and high-school context. Current roster rows also join same-season recorded production and shot profiles when the source IDs align. These records support recruiting research but do not assert commitment, transfer or eligibility status.
- The NCAA roster-intel toolbar downloads the exact season Parquet release from hash-addressed private R2, with the active D1 source receipt determining the object and ETag. It also exports every matching filtered roster row through a bounded paginated D1 path, retaining raw profile/shooting JSON and canonical NCAA player/team URLs. This keeps source retrieval auditable without exposing unverified public copies.
- NCAA roster IDs are retained as source-season keys; the archive does not infer cross-season person identity from names or unstable IDs. The ESPN-derived recruiting board is the separate source-stable view for exact-ID prior-program observations where that source supplies a matching athlete ID.
- The [high-school pipeline](/basketball/ncaa-high-schools/) aggregates those roster rows by source-supplied high-school label and shows represented players, programs, games and points, with a direct link back to the underlying roster search. It can export every matching school label in the bounded filtered cohort, including the active metric and an absolute roster-evidence URL. It is a descriptive source view, not a recruiting ranking.
- The [NCAA shooting profiles](/basketball/ncaa-shooting/) archive aggregates 4.88 million attributed shot records into 37,762 player/team profiles across 2019–20 through 2025–26. Profiles retain attempts, makes, points, shot zones, shot types and recorded distance; they remain in the NCAA identity namespace. The board exports both the visible page and every matching filtered profile through a bounded paginated D1 export, retaining raw zone JSON and canonical NCAA player/team URLs.
- The NCAA shooting toolbar also downloads each retained 2019–20 through 2025–26 shot release from hash-addressed private R2, using the matching D1 receipt rather than a mutable filename.
- The [NCAA player card](/basketball/ncaa-player/) joins only the same NCAA source player ID across season summaries, roster/high-school fields, shooting profiles, league-wide RAPM and the latest game rows. It is a source-native research handoff, not an ESPN identity crosswalk or a current eligibility claim. Its selected-season receipt panel links each retained evidence release with the retrieval clock and SHA-256. A live ranking snapshot on the card checks that exact ID against points per game, true shooting, balanced production and impact + production boards, showing the full qualified cohort and explicit sample thresholds; a missing placement stays an unqualified or unavailable observation.
- Each NCAA player card keeps the fast view to the latest 12 contests, offers an explicit complete-log view, and provides a full-season CSV export from a bounded D1 endpoint. The card also summarizes a newest-five source-row form window against the preceding five rows, pooling only complete points, minutes and shooting denominators; it is descriptive preparation context, not a projection. The complete view and export retrieve every retained contest for that source ID; the export includes every source stat key present in the season and preserves the raw source JSON for audit work.
- The [NCAA national leaderboard](/basketball/ncaa/) preserves the complete row returned for each cached final ranking measure, including publisher headers, cells, rank and value. The normalized columns drive sorting; the source-row evidence and CSV export keep additional publisher fields available without interpreting or discarding them. Missing measures remain unavailable when robots policy or the source release provides no row.
- The NCAA player box archive now includes an expandable retained-field view for possession, shooting, transition, shot-profile and assisted/unassisted scoring fields on every row, plus a readable view of every retained source key and the full source JSON. It exposes the source values already present in the archive while keeping unavailable fields distinct from zero.
- The [play-by-play archive](/basketball/pbp/) indexes 15.76 million SportsDataverse events across 45,391 source games from 2019–20 through 2025–26, with event, scoring-play and shooting-play counts plus direct publisher links. Its 2019–23 compact index does not claim box-score shot reconciliation; the 2024–26 shooting editions retain accepted attempts and exact reconciliation fields. Each retained attributed parquet can be downloaded from the archive toolbar through a hash-addressed Worker route backed by private R2; the public catalog remains the source of truth for the release hash.
- The [five-v-five matchup archive](/basketball/matchup-stints/) covers 1,535,739 source matchup-stint rows, 47,008 source contests and 6,582,191 source possessions across season-ending years 2019–26. Each season publishes the 5,000 highest-possession lineup pairs with repeat-game counts, shared minutes, points and home-versus-away scoring margin; source-native player labels remain unjoined. The public derivative is intentionally capped for fast research, while every source parquet and retrieval receipt is retained in R2 and the archive toolbar provides a hash-addressed source download.
- 1,629 published 2026–27 games, including 1,579 primary forecasts and 50 separately labeled cold-start estimates. This is a partial schedule.
- 5,461 players listed under 2026–27 in the source, spanning 354 programs. Listings are unconfirmed and may carry over; they do not prove current eligibility or a roster return.
- 30,955 source roster observations across the available 2024–25 through 2026–27 snapshots. The workload lab uses the 2026–27 snapshot; the earlier release is retained as historical roster context. The publisher does not provide a 2023–24 roster file in this source family.
- 1,595 different-program records in the historical 2024–25 → 2025–26 appearance comparison. These are observed participation changes, not claims about transfer announcements or reasons.
- 8,352 compact team-season standings records across 2002–03 through 2025–26. The archive keeps overall and conference records, scoring, point differential, streak and publisher display values from the attributed SportsDataverse standings release; it does not enter the Silvermine forecast.

## Source policy and identifiers

All new imports use the SportsDataverse GitHub release store. The publisher labels datasets CC BY 4.0. Attribution, source URLs, retrieval times, ETags, last-modified metadata and SHA-256 hashes are retained. Direct ESPN extraction remains disabled; NCAA requests must pass robots checks and currently cannot proceed.

The recruiting wire uses the published ESPN and NCAA.com men’s-basketball RSS feeds for current editorial context. The refresh stores each feed-supplied headline, summary and URL without fetching or rewriting linked article pages, stamps its retrieval time, and labels the publisher. It does not turn a headline into a transaction, eligibility or availability claim. The scheduled research workflow refreshes these feeds before basketball publication.

Parquet downloads substantially reduce network transfer. The shared release client maintains polite request spacing, conditional caching and bounded retries. Every published release used for this edition is listed in the model notebook.

The NCAA player-box ingester keeps every numeric source field on valid identified rows, including fields added by a later publisher release; its explicit metadata keys remain separate. Unknown non-numeric labels stay in the retained unresolved payload rather than being guessed into a stat.


Stable ESPN athlete IDs join game appearances to roster listings. Missing player IDs are preserved in `bb_unresolved`, not fabricated or inferred from names. NCAA RAPM IDs are not joined by name to ESPN IDs. The available `mbb_player_crosswalk` release was inspected but does not supply a verified NCAA-to-ESPN mapping, so it is not used for that purpose.

Public roster profiles retain basketball-relevant fields. Age and birth date are excluded. Team-attributed placeholder rows (for example, an ESPN display name of “Team”) remain in the raw warehouse but are excluded from the public player derivative. Historical program comparisons use actual recorded minutes, rather than mutable prior-season roster listings. A player absent from a partial listing is never labeled as departed. Multiple program affiliations are marked ambiguous. “New to dataset” does not mean freshman or new recruit.

## Independent efficiency model

The [weekly model experiment](BASKETBALL_EVALUATION.md) adds chronological 2024–25 and 2025–26 replays, with two independent season transitions compared against the preseason baseline. It publishes every prediction, weekly coefficient set and training cutoff, with interactive diagnostics and a verified R2 evidence archive. It remains separate from live forecasts and the prospective ledger.

`basketball_model.py` trains a ridge model on offensive efficiency for both sides of each eligible game. Estimated possessions are the average of each side's `FGA + 0.475 × FTA − ORB + TO`. Points per 100 estimated possessions are the response. Separate team offense, opponent defense and home-floor features are used. A second ridge model fits tempo from both team identities.

Efficiency regularization is fixed at 12; tempo regularization at 8. Older seasons receive weights of `0.6 ** age_in_seasons`. A program must have at least ten observed games in the latest fitting season to enter the model. The fitted field currently has 366 programs. This empirical eligibility rule is disclosed rather than represented as a verified complete Division I membership list.

For the 2026–27 forecast:

1. Fit initial efficiency and tempo coefficients on the 2023–24 release, retaining 2023–24 through 2025–26 in the production training archive.
2. Use 2024–25 predictions to calibrate a two-parameter logistic margin-to-win-probability mapping. The 80th percentile of absolute errors defines a symmetric nominal 80% interval.
3. Fit evaluation coefficients on 2023–24 and 2024–25, keeping calibration fixed. Evaluate 2025–26 without feeding that season's results into the evaluation model. A separate replay calibrates on 2023–24 and independently evaluates 2024–25.
4. Fit production coefficients on all four completed seasons (2022–23 through 2025–26). Apply the fixed calibration and publish future game forecasts with immutable model IDs and timestamps.

The 2025–26 test covers 5,734 games and excludes 564 paired-box games involving teams outside the trained field. Metrics: 67.21% winner accuracy, 10.39-point margin MAE, 13.26-point margin RMSE, 15.55-point total MAE, 0.2045 Brier score, 0.5911 log loss and 79.02% empirical coverage for nominal 80% ranges. The constant-home-margin baseline MAE is 11.96 points. The separate 2024–25 holdout covers 5,701 games; its weekly challenger margin MAE is 9.67 points versus 10.19 for the preseason fit, with 70.36% winner accuracy. These are retrospective results, not a market-edge claim.

Possession pace is normalized to 40 minutes using the final period count, including overtime. Forecasts use regulation pace; evaluation compares against final scores, including overtime. No injury, transfer, roster or recruiting features are used in the primary model. Games involving a program outside the trained field receive a separate cold-start estimate built from latest-season team priors shrunk toward the league mean; its interval is calibrated on held-out games and the estimate is not registered in the prospective ledger. Source corrections may have been published after the historical events.

The separate [roster continuity challenger](/basketball/model/) fits a chronological ridge model on prior descriptive net efficiency, exact-athlete-ID returning and represented prior-minute shares, incoming workload and listed-player count. It evaluates the 2026 transition against a prior-net baseline, then publishes a research-only margin delta for 2026–27 games when both source-listed teams have usable features. The featured basketball cards, matchup desk and game-plan workbench expose that delta beside the primary estimate. Roster snapshots have no verified pre-season publication clock, only two historical transitions are available, and the challenger does not change primary probabilities, uncertainty or ledger registrations.

## Player and team statistics

The [historical player archive](BASKETBALL_CAREERS.md) adds 24 published seasons, annual stat rankings, season/program production histories and 3.68 million retained identified box-score rows. Per-season coverage and source-identity warnings are explicit; early seasons are sparse. The archive coverage drawer now reports field-level completeness for all 17 retained box-score fields, with source-row and appearance-row denominators. ESPN-derived source profiles are shown alongside a player's history for position, listed size, experience and status context, while retaining the source-ID limitation. The profile browser supports shareable season/search/position/status URLs, page and complete filtered CSV exports with ESPN source URLs and raw profile JSON, and links each source row to the program dossier and a dated recruiting-evidence search. The player desk also links to the exact season player-box Parquet release, archived by receipt hash in private R2 and streamed through the read-only careers API. The historical NCAA leaderboard adds accuracy, per-40, stocks, creation and shot-profile rate boards with explicit denominator checks.

The [school announcement board](BASKETBALL_RECRUITING.md) adds dated recruiting evidence with historical-stat links and later availability statements. It is a partial editorial review, separate from unconfirmed source rosters.

The [roster impact lab](BASKETBALL_ROSTER_LAB.md) puts the source-listed 2026–27 roster snapshot beside exact-ID prior minutes, independent adjusted net rating and schedule coverage for every observed program. Returning, represented, incoming and unrepresented workload measures remain separate descriptive signals; unrepresented prior minutes are a source-review queue, not a departure count. None alter forecasts or establish transfer status, eligibility or departures.

The [team-stat browser](/basketball/team-stats/) exposes the attributed team-season archive across general, offensive and defensive categories, with page and full-filter CSV exports that preserve source field metadata, display values and raw numeric values. The [standings archive](/basketball/standings/) adds searchable overall and conference records, scoring, point differential and publisher display values across 24 seasons. The [boutique model archive](/basketball/boutique/) adds publisher adjusted ratings and Box Plus/Minus alongside the independent Silvermine model. Current team rows include a rank delta when the source ID has an audited Silvermine crosswalk, making model disagreement a review prompt rather than a blended grade. The page also documents the KenPom premium-source boundary and links to the source-native connector; no credentials or proprietary ratings are ingested. The [lineup lab](/basketball/lineups/) now spans eight NCAA-derived season-ending years (2019–26, covering 2018–19 through 2025–26) and aggregates lineup stints with explicit possession and games thresholds, offensive/defensive ratings and net performance. Its season, metric, threshold, search and sort controls produce shareable coaching links, and its toolbar downloads the exact D1-receipted source parquet from private R2. These products preserve source values and labels and do not feed those fields into Silvermine's adjusted ratings. The [shooting lab](BASKETBALL_SHOOTING.md) adds 738,233 field-goal attempts from 2.9 million bulk play-by-play events, with program/player court maps, shot labels, event logs and exact box-score reconciliation. Its source release is archived in R2; versioned shot evidence is served from D1.

The team-stat browser keeps its season, source field, direction, program search and page controls in the URL and offers a copy-link action, allowing an exact publisher field slice to be reopened for staff review.

The [NCAA team box archive](/basketball/ncaa-team-box/) adds 17 season-ending editions from 2010–11 through 2025–26, covering 341–365 source teams and 8,000–12,000 attributed team-game rows per season. It recomputes points-per-possession ratings, tempo and Four Factor rates from the source counts, preserves source totals and averages, and keeps descriptive team profiles separate from opponent-adjusted ratings. Season coverage reports unique source contests; team-game rows retain the source team attribution. The archive toolbar also downloads each retained source parquet through a hash-addressed private-R2 route.

The [program scouting library and matchup workbench](BASKETBALL_SCOUTING.md) add 366 native dossiers, historical window comparisons, game efficiency charts, personnel workloads and venue scenarios. The [five-v-five matchup archive](/basketball/matchup-stints/) adds the opposing lineup actually on the floor for source-derived possession samples, with shareable season, search, threshold and sort controls. The lineup lab can export every matching aggregate in its bounded filtered cohort, retaining all lineup slots, sample counts, ratings and raw source stats JSON. The matchup desk also preserves the selected page in its shareable URL, so a staff handoff reopens the same filtered slice rather than silently returning to page one. These use the existing source/model edition and preserve the distinction between historical observations and future predictions. NCAA player rows in the career, roster, shooting, ranking, box-score and impact boards expose the publisher's direct player URL alongside the internal archive handoff; this is source navigation, not an identity crosswalk.

The [expanded matchup briefs](BASKETBALL_BRIEFS.md) connect all 1,579 current forecasts to team-specific Four Factor contrasts, historical personnel, dated school announcements and matching ledger records. They also summarize exact-ID 2026–27 roster observations and the share of prior minutes those listings represent, while keeping eligibility and availability explicitly unconfirmed. The matchup desk surfaces that workload context beside each forecast as descriptive evidence; it does not silently alter model scores. Venue-preserving workbench links, browser preparation notes and print layouts support the coaching workflow.

The [forecast lab](/basketball/forecast-lab/) ranks the upcoming slate by disagreement between the primary efficiency forecast and the research-only roster-continuity scenario. It also filters cold-start estimates and verified market observations, displays the primary interval and confidence, surfaces the latest verified spread, total and no-vig moneyline edges in the comparison table, and exports the exact comparison rows. Its manual line checker keeps the selected game in the shareable URL alongside the team, view and sort filters, fetches the exact live D1 forecast for that game when available and records the model values used for each local quote, so a browser-only market review can be reopened with the same model context. Scenario deltas are descriptive research prompts; they do not change primary probabilities, intervals or ledger registrations. The journal publishes a static [`/feed.xml`](/feed.xml) RSS feed with the field guides and current football and basketball forecast briefs, so readers can follow new research without treating the feed as a live odds or eligibility source.

The recruiting wire now archives the permitted ESPN and NCAA.com RSS release in Cloudflare D1 (`bb_news_articles` and `bb_news_releases`). The public Worker endpoint `/api/basketball/research/news` supports bounded search, pagination and release metadata; the browser refreshes the wire from that archive and falls back to the bundled release if the API is unavailable. Only publisher-supplied headline, summary, date, categories, author and source URL are retained; linked article bodies are never fetched. Article identity is the feed GUID hash, so each daily sync is idempotent and preserves first/last observation clocks.

The player index exposes PPG, RPG, APG, steals, blocks, turnovers, minutes, eFG%, estimated TS% and 3P%. Default qualification is 15 games and 400 recorded minutes, with complete fields. DNP rows remain accessible in game logs. TS uses the same disclosed 0.475 college free-throw coefficient. The index also offers an all-around profile index: within-cohort percentiles for PPG, RPG, APG, steals, blocks, TS%, eFG% and turnover rate (lower is favorable), requiring four observed components and retaining the component count beside each tied rank. It is a transparent shortlist aid, not a value model or forecast input. The national leaders page exposes eight numeric publisher metrics from the attributed 2025–26 release, with source display values and a 15-game minimum preserved separately from Silvermine-derived rankings. It also publishes qualified Box BPM, Offensive BPM and Defensive BPM boards from the separate player-value release, requiring 400 recorded minutes and retaining publisher IDs, minutes and values. The source-stat browser exposes all 44 retained publisher fields across both available 2024–25 and 2025–26 source seasons through a paginated D1 API; compound made-attempted fields remain the publisher's display strings, every row links back to the player source record, a bounded minimum-games filter makes small samples explicit without deleting them from the archive, and a bounded full-export action downloads every matching row with the active source receipt.

Team ratings show adjusted offense, defense, net efficiency and tempo, plus pooled observed four factors. They also publish opponent-adjusted offensive and defensive eFG%, turnover, offensive-rebound and free-throw rates using the same team/venue ridge design with recency weights through 2025–26. Lower adjusted defensive eFG%, turnover, ORB and free-throw rates are better; lower offensive turnover rate is better. Schedule strength is the mean adjusted net strength of rated opponents; the number of included opponents is reported. Search and sort selections persist in the URL (`q` and `sort`) and the ratings toolbar can copy the exact board link for staff handoff. These are independent Silvermine calculations, not KenPom or another publisher's proprietary ratings.

Player impact uses the publisher's league-wide NCAA stint-ridge RAPM. Net RAPM is ORAPM + DRAPM, with a display qualification of at least 500 possessions at each end. The [within-team RAPM archive](/basketball/impact/within-team/) adds 67,554 source player/team rows across 2010–11 through 2025–26, with source team possession samples, model-player counts and a separate season-qualified board. It is kept separate from league-wide RAPM and is descriptive; neither impact release feeds the Silvermine forecast or creates an identity join. Each within-team season also exposes its retained source parquet through the archive toolbar. These are distinct data products and identity namespaces. The site links to the publisher's `hoopR/R/load_ncaa_mbb.R` documentation.

## Storage and refresh

D1 migrations `0009_basketball_research.sql`, `0017_basketball_team_season.sql`, `0018_basketball_boutique.sql`, `0019_basketball_lineups.sql`, `0020_basketball_player_core.sql`, `0021_basketball_ncaa_player_box.sql`, `0022_basketball_ncaa_rosters.sql`, `0023_basketball_ncaa_shooting.sql` and `0024_news_archive.sql` add separate `bb_*` tables without modifying football or legacy basketball tables. Compact player/team box-score fields are stored alongside published player-season, team-season, boutique-model, lineup, source-profile and NCAA player-season metrics, roster profiles, appearance summaries, shooting profiles, source receipts, model artifacts, forecast snapshots and attributed publisher-news records. The lineup warehouse contains 599,160 aggregates across season-ending years 2019–26; original lineup parquet sources and receipts are archived in R2. Matchup-stint, within-team RAPM and NCAA team-box parquet sources and receipts are archived in R2, while the compact per-season derivatives are served as Cloudflare Assets for fast filtering. Full source downloads are cached locally under ignored `.local/basketball/`; public derivative JSON is served by Cloudflare Assets.

```bash
PYTHONPATH=ncaa_scraper .venv/bin/python -m ncaa_scraper.basketball --sql .local/basketball.sql
PYTHONPATH=ncaa_scraper .venv/bin/python -m unittest discover -s ncaa_scraper/tests -p 'test_*ball.py'
.venv/bin/python scripts/publish-basketball.py
```

The publisher refreshes sources, rebuilds the player, play-by-play, five-v-five, NCAA team-box and within-team RAPM derivatives, writes the identity-review breakdown to `unresolved-coverage.json`, archives source parquet receipts in R2, tests, builds both frontend applications, syncs D1 and deploys the Worker using credentials supplied through `~/.env`. The repository also defines a serialized daily GitHub Actions refresh with manual sport selection in [`.github/workflows/refresh-research.yml`](../.github/workflows/refresh-research.yml); it requires the same Cloudflare credentials as repository secrets. Each workflow run now executes a release-health gate that checks generated timestamps, model IDs, forecast-count bounds, rating presence and NCAA-season alignment before deployment. Football and basketball refresh scripts rebuild the shared site while leaving the other sport's data edition intact.

`scripts/refresh-news-rss.py` refreshes the permitted feed fields and `scripts/sync-news.py` validates the release, applies `0024_news_archive.sql`, and upserts the current article/release snapshot in small idempotent D1 query batches. The basketball publisher invokes this synchronizer after each news refresh, so a transient D1 import failure can be resumed without duplicating stories.

The large basketball warehouse exports are streamed into hashed SQL batches capped at 7.5 MB, with a manifest beside `basketball.sql` and `ncaa-player-box-2026.sql`. `publish-basketball.py` verifies every batch before importing it into D1, retries transient Cloudflare import failures, and supports a resumed run with `BASKETBALL_SQL_BATCH_START` or `NCAA_PLAYER_BOX_SQL_BATCH_START` after a confirmed batch-level interruption. The same offsets are available as `--basketball-sql-batch-start N` and `--ncaa-player-box-sql-batch-start N`; `--help` exits before any refresh work. The first batch contains the scoped season deletes; later batches are idempotent `INSERT OR REPLACE` statements. After all syncs, `scripts/verify-basketball-d1.py` compares 16 artifact-backed D1 table counts in one remote query, including the current NCAA game release and historical NCAA season summaries, and stops publication on any mismatch.

## Release verification

The original basketball release passed ingestion/model tests, Worker API/routing tests, Worker type checking and the combined production build. Browser checks covered desktop/mobile layouts, player search, roster view switching, matchup filters and generated briefs. Live checks verified D1 counts, player game logs and season statistics, error responses, football access and legacy scouting redirects. The scouting extension adds seven Python tests and three frontend tests, including agreement with all 1,579 published basketball forecasts. The local Wrangler preview failed with a runtime spawn error, so Worker integration is verified on the deployed release. The first scheduled refresh completed successfully on September 7, 2026, including source refresh, publication validation, combined build and Cloudflare deployment; a subsequent manual run also completed successfully.

D1 coverage queries use a batch of individual counts to stay within its compound SELECT limit. Forecast counts include preserved model snapshots; the current model has 1,579 forecasts, while the release warehouse retains 28,422 basketball registrations across the preserved model fingerprints.

`GET /api/basketball/research/coverage` returns all 18 public basketball D1 table counts plus grouped `source_receipts` with each dataset's receipt count and latest `fetched_at` value. It also returns `location_validation` for the retained schedule: total rows, neutral-site rows, missing venue labels, unconfirmed starts, duplicate participant IDs, invalid period counts and completed rows missing a score. `possession_validation` applies the same FGA/FTA/ORB/TO, period, impossible shooting-total and pace guards used by the model to completed games and reports missing fields, negative values, nonpositive estimates, pace outliers, schedule/box-score mismatches and valid model-ready estimates. The model notebook surfaces this live D1 diagnostic beside the published formula, so readers can inspect the evidence boundary where they learn the method. This is a remote D1 freshness and schedule-integrity check; the homepage receipt cards are the corresponding static edition view.

`GET /api/basketball/research/forecasts?season=2027&status=upcoming&q=Duke&limit=50` exposes the D1-backed forecast record for integrations and reproducible research. It joins each stored prediction to its exact schedule row, returns the model ID and creation clock, and supports `all`, `upcoming` or `completed` status, the newest model by default (`model=latest`), `model=all` or an exact model ID, literal team-name search, an exact numeric `gameId` lookup, bounded pagination and a `meta=1` catalog of seasons and model editions. The metadata catalog includes each edition's model creation/cutoff clocks, target season, training-game count and seasons, calibration sample/range and holdout metrics without exposing coefficient vectors. The JSON payload is parsed from the stored forecast artifact; malformed payloads are withheld as `prediction: null` rather than making an otherwise valid page fail. This endpoint does not create, revise or recompute forecasts.

The forecast lab also reads the `meta=1` catalog from the deployed Worker after hydration. It compares the page's static model ID with the latest D1 edition and shows the live forecast-row count and source clock, so a reader can spot a stale static release without treating the live check as a forecast recomputation.

`GET /api/basketball/research/unresolved?dataset=ncaa_player_box&season=2026&q=Duke&limit=40` returns a bounded page of source observations retained in `bb_unresolved`. Dataset, season, withholding-reason and source-JSON filters are parameterized; the response parses each original object into `source` and keeps the source row index. The endpoint is for audit and identity review only: it does not join, rank, forecast or assert a player/team identity.

The shared market archive keeps its sport, season, search and page controls in the URL and offers a copy-link action, so a line review can be reopened or handed to another researcher with the same slice of retained observations. The basketball matchup slate and forecast lab hydrate their market panels from the same live scorecard endpoint, then fall back to the bundled market map if the D1 request is unavailable; this keeps upcoming-game cards aligned with the scorecard’s eligibility rules.

The basketball landing page also reports the live scorecard’s qualifying quote count and forecast denominator. It links directly to the market archive and states the provider-clock, participant-match and pre-tip requirements, so an empty quote count is visible as missing licensed coverage rather than an implied line.

## Remaining full-goal work

- Configure an authorized recruiting provider key (the CBBD portal/player/team connector and CSV intake are ready) and ingest verified current recruiting/transfer/eligibility data. CBBD portal rows expose season and eligibility but no event date, so they remain separate from the dated announcement board until a dated source is supplied.
- Expand historical player-game seasons, add more possession and location validation, and extend the weekly experiment to additional prospective seasons.
- Extend the roster challenger and weekly experiment across additional dated transitions before considering any roster feature for the primary forecast. The [shared ledger](RESEARCH_LEDGER.md) implements prospective settlement and market comparisons; live feed validation and real future outcomes remain pending.
- Preserve completed matchup briefs and enrich major-game editorial analysis beyond generated statistical previews.
- Monitor scheduled refresh freshness against expected source coverage and investigate any failed publication run.
